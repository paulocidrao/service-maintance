import { defineAppConfig } from "nuxt/app";

export default defineAppConfig({
  ui: {
    colors: {
      primary: "primary",
      secondary: "secondary",
      muted: "muted",
      accent: "accent",
      destructive: "destructive",
      info: "blue",
      success: "green",
      warning: "yellow",
      error: "red",
      white: "white",
    },
  },
});
